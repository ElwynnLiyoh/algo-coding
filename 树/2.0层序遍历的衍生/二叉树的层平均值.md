---
title: 二叉树的层平均值
time: 2026-09-05 Sat 00:37
tags:
---

## 二叉树的层平均值

题号： LC637
描述： 给定一个非空二叉树的根节点 `root` , 以数组的形式返回每一层节点的平均值。与实际答案相差 `10-5` 以内的答案可以被接受。

### 广度优先/队列

时间复杂度： $O(n)$，需要访问每一个节点
空间复杂度： $O(qsize)$

```python
import queue
class Solution:
    def averageOfLevels(self, root: Optional[TreeNode]) -> List[float]:
        if not root:
            return 0

        q = queue.Queue()
        q.put(root)
        res = []
        while not q.empty():
            total = 0
            size = q.qsize()
            for _ in range(size):
                node = q.get()
                total += node.val
                if node.left:
                    q.put(node.left)
                if node.right:
                    q.put(node.right)
            res.append(total / size)
        return res
```



### 深度优先

时间复杂度： $O(n)$，需要访问每一个节点
空间复杂度： $O(n)$，最坏情况下二叉树的高度等于节点个数

```python
class Solution:
    def averageOfLevels(self, root: Optional[TreeNode]) -> List[float]:
        def dfs(root: TreeNode, level: int):
            if not root:
                return 0
            if len(totals) <= level:
              	# 新的一层
                totals.append(root.val)
                counts.append(1)
            else:
                totals[level] += root.val
                counts[level] += 1
            dfs(root.left, level + 1)
            dfs(root.right, level + 1)

        counts = list()
        totals = list()
        dfs(root, 0)
        return [total / count for total, count in zip(totals, counts)]
```

