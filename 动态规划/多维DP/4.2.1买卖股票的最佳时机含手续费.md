---
title: 买卖股票的最佳时机含手续费
time: 2026-09-20 Sun 15:52
tags:
  - DP
---

## 买卖股票的最佳时机含手续费

题号： LC714
描述： 给定一个整数数组 `prices`，其中 `prices[i]`表示第 `i` 天的股票价格 ；整数 `fee` 代表了交易股票的手续费用。
你可以无限次地完成交易，但是你每笔交易都需要付手续费。如果你已经购买了一个股票，在卖出它之前你就不能再继续购买股票了。

**思路**： 与股票2类似，差别仅是多了手续费，那么只需要在卖出的时候扣除手续费即可。

时间复杂度： $O(n)$
空间复杂度： $O(n)$

```python
class Solution:
    def maxProfit(self, prices: List[int], fee: int) -> int:
        n = len(prices)
        if n <= 1:
            return 0

        # dp[i][0]：第 i 天结束时，不持有股票的最大现金
        # dp[i][1]：第 i 天结束时，持有股票的最大现金
        dp = [[0] * 2 for _ in range(n)]
        dp[0][1] = -prices[0]
        
        for i in range(1, n):
            dp[i][0] = max(dp[i - 1][0], dp[i - 1][1] + prices[i] - fee) # 卖出时扣除手续费
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] - prices[i])

        return dp[-1][0]
```

由于只依赖前一天，故可以压缩

时间复杂度： $O(n)$
空间复杂度： $O(1)$

```python
class Solution:
    def maxProfit(self, prices: List[int], fee: int) -> int:
        cash = 0  # 当前不持股状态
        hold = -prices[0] # 当前持股状态

        for price in prices[1:]:
            cash = max(cash, hold + price - fee)
            # 直接使用了更新后的cash
            # 当cash使用了今天卖出的结果时，cash - price一定小于旧hold，因此不会改变hold。
            # 即hold 使用了更新后的 cash，不会产生一个比正确状态更优的非法结果。
            # 因为“今天卖出再买入”不会比昨天继续持有更赚钱，因为平白支付了一次手续费。
            hold = max(hold, cash - price)

        return cash
```