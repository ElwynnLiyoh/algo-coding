---
title: 主持人调度
time: 2026-09-01 Tue 17:42
tags:
  - 贪心
---

## 主持人调度

题号： BM96
描述： 
有 n 个活动即将举办，每个活动都有开始时间与活动的结束时间，第 i 个活动的开始时间是 starti ,第 i 个活动的结束时间是 endi ,举办某个活动就需要为该活动准备一个活动主持人。  

一位活动主持人在同一时间只能参与一个活动。并且活动主持人需要全程参与活动，换句话说，一个主持人参与了第 i 个活动，那么该主持人在 (starti,endi) 这个时间段不能参与其他任何活动。求为了成功举办这 n 个活动，最少需要多少名主持人。
思路： 
时间复杂度： $O(n \log n)$
空间复杂度： $O(n)$

### 方法一：排序 + 最小堆

```python
import heapq

def minmumNumberOfHost(self , n: int, startEnd: List[List[int]]) -> int:
    if n == 0:
        return 0

    # 按开始时间排序
    startEnd.sort(key=lambda x: x[0])

    # 最小堆：保存正在使用的主持人的活动结束时间
    heap = []

    for start, end in startEnd:
        # 如果最早结束的活动已经结束，
        # 该主持人可以继续主持当前活动
        if heap and heap[0] <= start:
            heapq.heappop(heap)

        # 当前活动占用一个主持人
        heapq.heappush(heap, end)

    return len(heap)
```

### 方法二：开始时间和结束时间分别排序

```python
def minmumNumberOfHost(self, n: int, startEnd: List[List[int]]) -> int:
    starts = sorted(interval[0] for interval in startEnd)
    ends = sorted(interval[1] for interval in startEnd)

    i, j = 0, 0
    hosts = 0

    for i in range(n):
        # 新开始的节目大于上一轮结束的时间，主持人不变
        if starts[i] >= ends[j]:
            j += 1
        else:
            # 主持人增加
            hosts += 1

    return hosts
```
