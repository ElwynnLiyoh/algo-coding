---
title: 链表相加
time: 2026-08-26 Wed 01:29
tags:
  - 反转
  - 模拟
---

## 链表相加

```go
func reverseList(head *ListNode) *ListNode {
    pre := (*ListNode)(nil)
    for head != nil {
        nx := head.Next
        head.Next = pre
        pre = head
        head = nx
    }
    return pre
}
 
func addInList(head1 *ListNode, head2 *ListNode) *ListNode {
    rev1 := reverseList(head1)
    rev2 := reverseList(head2)
    carryBit := 0
    dummy := new(ListNode)
    res := dummy
    for rev1 != nil || rev2 != nil {
        newNode := ListNode{Val: carryBit}
        if rev1 != nil {
            newNode.Val += rev1.Val
            rev1 = rev1.Next
        }
        if rev2 != nil {
            newNode.Val += rev2.Val
            rev2 = rev2.Next
        }
        carryBit = newNode.Val / 10
        newNode.Val %= 10
        res.Next  = &newNode
        res = res.Next
    }
    if carryBit == 1 {
        res.Next = &ListNode{Val: 1}
    }
    return reverseList(dummy.Next)
}

```

